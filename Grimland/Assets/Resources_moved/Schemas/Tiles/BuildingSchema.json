{
    "$schema": "http://json-schema.org/schema",
    "title": "Building Tile Requirement Schema",
    "description": "A specific schema that checks for required properties in YAML files",
    "required": [
        "MoveSpeedMultiplier",
        "ApplicableJobs",
        "Atlas",
        "Color",
        "MaxHealth"
    ]
}
