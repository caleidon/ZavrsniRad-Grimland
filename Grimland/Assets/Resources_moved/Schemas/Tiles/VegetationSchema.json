{
    "$schema": "http://json-schema.org/schema",
    "title": "Vegetation Requirement Schema",
    "description": "A specific schema that checks for required properties in YAML files",
    "required": [
        "MoveSpeedMultiplier",
        "GrowthSprites",
        "ApplicableJobs",
        "MaxHealth"
    ]
}
