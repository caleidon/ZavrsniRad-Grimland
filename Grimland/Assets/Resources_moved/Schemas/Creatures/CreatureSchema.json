{
    "$schema": "http://json-schema.org/schema",
    "title": "Creature Requirement Schema",
    "description": "A specific schema that checks for required properties in YAML files",
    "required": ["BaseMovementSpeed", "Sprite", "Behavior"]
}
