{
    "$schema": "http://json-schema.org/schema",
    "title": "Base Tile Requirement Schema",
    "description": "A specific schema that checks for required properties in YAML files",
    "required": ["MoveSpeedMultiplier", "Fertility", "GenHeight", "Sprite"]
}
