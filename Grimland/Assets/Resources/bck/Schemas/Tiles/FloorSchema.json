{
    "$schema": "http://json-schema.org/schema",
    "title": "Floor Requirement Schema",
    "description": "A specific schema that checks for required properties in YAML files",
    "required": ["MoveSpeedMultiplier", "Sprite", "ApplicableJobs", "MaxHealth"]
}
